Dispose generated assemblies
allow continuous conversation
allow custom enums in assistant APIs
allow APIs returning information that can be fed back to AI

Add the ability to specify persona
Verify that at least one and only one assistant method is called when code is sent.
Manage prompt budget